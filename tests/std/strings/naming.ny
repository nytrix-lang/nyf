; allow those types of functions and variables

;fn kebab-case() {
;    return 1
;}
;
;fn do->predicate() {
;    return 1
;}
;
;fn predicate?() {
;    return 1
;}

;assert(kebab_case() == 1)
;assert(do->predicate() == 1)
;assert(predicate?() == 1)
print("✓ std.strings.naming tests passed")
